# ==============================================================================
# Workflow Service — Kubernetes Deployment
# ==============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-service
  namespace: nemo
  labels:
    app: workflow-service
    app.kubernetes.io/name: workflow-service
    app.kubernetes.io/part-of: nemo
    app.kubernetes.io/component: backend
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: workflow-service
  template:
    metadata:
      labels:
        app: workflow-service
        app.kubernetes.io/name: workflow-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8003"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: nemo-workflow-service
      terminationGracePeriodSeconds: 30
      containers:
        - name: workflow-service
          image: ghcr.io/jagadeeshm007/nemo/workflow-service:latest
          ports:
            - name: http
              containerPort: 8003
              protocol: TCP
          env:
            - name: NEMO_ENV
              value: production
            - name: POSTGRES_HOST
              valueFrom:
                secretKeyRef:
                  name: nemo-db-credentials
                  key: host
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: nemo-db-credentials
                  key: password
            - name: REDIS_HOST
              valueFrom:
                secretKeyRef:
                  name: nemo-redis-credentials
                  key: host
            - name: KAFKA_BROKERS
              valueFrom:
                configMapKeyRef:
                  name: nemo-infra-config
                  key: kafka-brokers
            - name: TEMPORAL_HOST
              value: temporal.nemo-infra:7233
            - name: TEMPORAL_NAMESPACE
              value: nemo
            - name: VAULT_ADDR
              value: http://vault.nemo-infra:8200
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://otel-collector.nemo-monitoring:4317
            - name: OTEL_SERVICE_NAME
              value: workflow-service
            - name: UNLEASH_URL
              value: http://unleash.nemo-infra:4242/api
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: workflow-service
  namespace: nemo
  labels:
    app: workflow-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8003
      targetPort: http
  selector:
    app: workflow-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: workflow-service
  namespace: nemo
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: workflow-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: workflow-service
  namespace: nemo
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: workflow-service
